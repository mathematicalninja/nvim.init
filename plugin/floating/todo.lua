--{{{ TODOs
-- vim: set foldmethod=marker
--[[ TODO:
     1. quickfix floater
     2. pipe test results into floater
     3. preview float for generated images/pdfs/etc. 
     4. Household notes as an EDITABLE file.
     5. Todos buffer
     6. project notes file. (let user pass in a default (relative) path)
     ==> e.g. I could pass in "ideas.txt" and this would open that.
     7. open a specific file in a float. With filename prompt? Oil?

--]]

-- TODO: add in a floating "man", fzf/telescope into buf
-- TODO: add in a floating "help", fzf/telescope into buf
-- NOTE: width should be 78 for help

--[[ TODO:
     1. On Win Open, call attach function.
     2. on Win Close, call cleanup function.
     3. Cleanup ==> reposition windows (optional)
     4. move "positional logic" into apply_style, allowing different styles to micromanage.
     5. Cleanup ==> state update
     6. modularise ==> float:toggle(scratch,autoposition=fn())
     ==> scratch: {bufwin} -> bufwin
     7. Cleanup modularity, M.[-] imports.

--]]
--}}}
